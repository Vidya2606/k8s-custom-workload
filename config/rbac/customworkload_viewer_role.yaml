# This rule is not used by the project k8s-custom-workload-init itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to apps.example.com resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: k8s-custom-workload-init
    app.kubernetes.io/managed-by: kustomize
  name: customworkload-viewer-role
rules:
- apiGroups:
  - apps.example.com
  resources:
  - customworkloads
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - apps.example.com
  resources:
  - customworkloads/status
  verbs:
  - get
